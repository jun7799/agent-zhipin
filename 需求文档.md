# 背景
现在boss直聘太复杂
招聘方要上传一堆信息
而且费用高。

应聘方要上传一堆信息
查询麻烦
也不支持agent代理检索
严格的爬虫防范。

# 功能
## 用人端：
想弄一个数据库
用来存用人端的招聘信息
（具体字段要设计一下）

然后用人端通过api来上传自己的招聘信息。
后面我应该会写一个skill，引导它们上传。这个暂时不用管。后面再说。

## 应聘端：
找工作的小伙伴，可以通过查询接口
输入必要的筛选条件，查询目标城市的岗位信息。
同样也是通过API调用的方式。
（筛选条件要定义一下，当然是跟进岗位信息的字段来定了）

同样后续应该也会有skill。
然后每次返回的岗位数量，可能要做一些限制。
不然用户将我的库给导出了。

# 可行性
你评估下

# 盈利点分析
你评估下
能保持项目正常运行就行。

