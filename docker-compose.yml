version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/agent_zhipin.db
      - JWT_SECRET_KEY=dev-secret-key-change-me
      - DEBUG=true
    volumes:
      - ./app:/app/app
      - ./data:/app/data
